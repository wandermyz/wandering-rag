# Qdrant vector database configuration
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Note: This project uses sentence-transformers/paraphrase-multilingual-mpnet-base-v2
# for generating multilingual embeddings (including Chinese support).
# No API key is required as embeddings are generated locally. 

# Notion API credentials
NOTION_TOKEN=your_notion_integration_token_here

# Notion databases to index (format: name:database_id, comma-separated)
# Example: Personal:abc123,Work:def456
NOTION_DATABASES=database_name:database_id

# Obsidian configuration
# Path to obsidian vaults, comma-separated
OBSIDIAN_VAULTS=your_obsidian_folder_path
